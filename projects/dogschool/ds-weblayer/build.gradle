apply plugin: 'war'

war { archiveName 'ds-weblayer.war' }

dependencies {
	providedCompile project(':ds-ejbservice')
	providedCompile group: 'javax.servlet', name:'javax.servlet-api', version:'3.0.1'
}